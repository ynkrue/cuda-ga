# Lennard-Jones cluster parallel-tempering basin hopping
# Lines: key = value

n_atoms = 147
n_walkers = 2048
iterations = 500

# temperature ladder
n_temps = 32          # replicas per ensemble
T_min = 0.25          # coldest rung (refines)
T_max = 6             # hottest rung (crosses funnel barriers)

step_size = 1.0       # base displacement; each replica uses step_size * T
seed = 42
